<!DOCTYPE html>
<html lang="pl">
    <head>
        <title> Gamex - blog for real gamers </title>
        <meta charset="utf-8" />
        <base href="/">
        <link id="pagestyle" rel="stylesheet" href="./css/indexstyle.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        
    </head>

    <body>
        <%- include("./../navbar") %>
        <div class="parallax"><img id="logo" src="./images/logo-trans.png"></div>
        <img style="margin-top:25%; width:100%; height:50vh; position: flex;" src="<%= article.banner %>">
        <div class="container mt-5">
            <h1 style="margin-top: 10%; color:white; max-width:70vw;"><%= article.title %></h1>
            <div style="color:rgb(133, 133, 133)"><%= article.createdAt.toLocaleDateString() %> at <%= article.createdAt.getHours() + ":" + ("0" + article.createdAt.getMinutes()).slice(-2) + ":" + ("0" + article.createdAt.getSeconds()).slice(-2) %> by <%= article.createdByName %></div>
            <div style="margin-top: 6%; color:white; max-width:70vw; text-align:justify; text-justify:auto;"><%= article.description %></div>
            <div class="d-flex mt-5 w-100">
                <a href="/" class="btn btn-danger" style="margin-right: 2%;">Back</a>
                <% if(user != null && user.id == article.createdByID) { %>
                <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
                <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="w-100">
                    <button type="submit" class="btn btn-danger" style="margin-left: 80%;">Delete</button>
                </form>
                <% } %>
            </div>


            

        </div>
        <div class="pause"></div>
        <div class="parallax"></div>
            
        </body>
        <script src="./scripts/random_background.js"></script>
        </html>